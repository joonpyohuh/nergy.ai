# Server-only (Vite middleware / Vercel /api/*). Do NOT use VITE_ prefix.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.5

# Shared team password gate
TEAM_PASSWORD=
SESSION_SECRET=

# Supabase (service role — server only)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
